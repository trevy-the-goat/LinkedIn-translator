# LinkedIn Translator — LLM integration (optional)
# If a key is present, translations are generated by the provider and the
# built-in mock engine acts as a fallback on failure.
# Without any key, the offline "PowerPhrase 4.2 Turbo" mock engine is used.

VITE_OPENAI_API_KEY=
VITE_ANTHROPIC_API_KEY=
